name: Publish to npm and ClawHub

on:
  workflow_dispatch:
    inputs:
      publish_target:
        description: Package(s) to publish
        required: true
        default: cli
        type: choice
        options:
          - cli
          - schemas
          - all
      clawhub_changelog:
        description: ClawHub changelog text for the Xerg skill
        required: false
        default: ''
        type: string
      clawhub_tags:
        description: ClawHub tags for the Xerg skill
        required: true
        default: latest,audit,cli,cost,efficiency,finops,openclaw,waste
        type: string

jobs:
  verify:
    runs-on: ubuntu-latest
    permissions:
      contents: read

    steps:
      - uses: actions/checkout@v5

      - uses: actions/setup-node@v5
        with:
          node-version-file: .nvmrc
          cache: pnpm
          registry-url: https://registry.npmjs.org

      - name: Install pnpm
        run: npm install --global pnpm@10.6.2

      - name: Install
        run: pnpm install --frozen-lockfile

      - name: Lint
        run: pnpm lint

      - name: Build schemas
        run: pnpm --filter @xerg/schemas build

      - name: Typecheck
        run: pnpm typecheck

      - name: Test
        run: pnpm test

      - name: Build
        run: pnpm build

      - name: CLI package smoke test
        run: pnpm cli:smoke

  publish-schemas:
    if: ${{ github.event.inputs.publish_target == 'schemas' || github.event.inputs.publish_target == 'all' }}
    runs-on: ubuntu-latest
    needs: verify
    permissions:
      contents: read
      id-token: write

    steps:
      - uses: actions/checkout@v5

      - uses: actions/setup-node@v5
        with:
          node-version-file: .nvmrc
          cache: pnpm
          registry-url: https://registry.npmjs.org

      - name: Install pnpm
        run: npm install --global pnpm@10.6.2

      - name: Install
        run: pnpm install --frozen-lockfile

      - name: Build schemas
        run: pnpm --filter @xerg/schemas build

      - name: Publish @xerg/schemas
        working-directory: packages/schemas
        run: npm publish --access public

  publish-cli:
    if: ${{ github.event.inputs.publish_target == 'cli' || github.event.inputs.publish_target == 'all' }}
    runs-on: ubuntu-latest
    needs: verify
    permissions:
      contents: read
      id-token: write

    steps:
      - uses: actions/checkout@v5

      - uses: actions/setup-node@v5
        with:
          node-version-file: .nvmrc
          cache: pnpm
          registry-url: https://registry.npmjs.org

      - name: Install pnpm
        run: npm install --global pnpm@10.6.2

      - name: Install
        run: pnpm install --frozen-lockfile

      - name: Build
        run: pnpm build

      - name: Prepare ClawHub publish metadata
        id: clawhub_meta
        env:
          INPUT_CHANGELOG: ${{ github.event.inputs.clawhub_changelog }}
        run: |
          VERSION="$(node -p "require('./packages/cli/package.json').version")"
          CHANGELOG="$INPUT_CHANGELOG"

          if [ -z "$CHANGELOG" ]; then
            CHANGELOG="Aligned with @xerg/cli $VERSION."
          fi

          {
            echo "version=$VERSION"
            echo "changelog<<EOF"
            echo "$CHANGELOG"
            echo "EOF"
          } >> "$GITHUB_OUTPUT"

      - name: Verify ClawHub token is configured
        env:
          CLAWHUB_TOKEN: ${{ secrets.CLAWHUB_TOKEN }}
        run: |
          if [ -z "$CLAWHUB_TOKEN" ]; then
            echo "Missing CLAWHUB_TOKEN GitHub Actions secret." >&2
            echo "Add it before publishing @xerg/cli so npm and ClawHub stay in sync." >&2
            exit 1
          fi

      - name: Install ClawHub CLI
        run: npm install --global clawhub@0.9.0

      - name: Log in to ClawHub
        env:
          CLAWHUB_TOKEN: ${{ secrets.CLAWHUB_TOKEN }}
        run: clawhub login --token "$CLAWHUB_TOKEN" --no-browser

      - name: Publish Xerg skill to ClawHub
        env:
          XERG_SKILL_VERSION: ${{ steps.clawhub_meta.outputs.version }}
          XERG_SKILL_CHANGELOG: ${{ steps.clawhub_meta.outputs.changelog }}
          XERG_SKILL_TAGS: ${{ github.event.inputs.clawhub_tags }}
        run: |
          clawhub --no-input publish ./skills/xerg \
            --slug xerg \
            --name "Xerg" \
            --version "$XERG_SKILL_VERSION" \
            --changelog "$XERG_SKILL_CHANGELOG" \
            --tags "$XERG_SKILL_TAGS"

      - name: Publish @xerg/cli
        working-directory: packages/cli
        run: npm publish --access public
