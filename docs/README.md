# Xerg Docs

`docs/v2` is the current source of truth for product direction, build scope, and pricing posture.

`docs/v1` is the archived first-pass planning set. It is kept for context, not for implementation decisions.

If `v1` and `v2` disagree, follow `v2`.
