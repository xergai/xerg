---
title: Xerg
description: Audit OpenClaw agent spend in dollars, compare before and after changes, run remote audits, and gate CI on waste thresholds.
---

Xerg audits OpenClaw usage in dollars rather than tokens. It reads OpenClaw gateway logs and session transcripts, normalizes runs locally, and surfaces where spend is leaking.

## What Xerg does

- Audits local OpenClaw activity from default paths or explicit files.
- Compares the current audit with the newest compatible cached snapshot.
- Audits SSH and Railway targets from your local machine.
- Exports human-readable, Markdown, and machine-readable JSON output.
- Pushes summaries to the Xerg API and can fail CI when confirmed waste is above a threshold.

## What Xerg surfaces

Xerg splits findings into two buckets:

- Confirmed waste: `retry-waste` and `loop-waste`
- Savings opportunities: `context-outlier`, `idle-spend`, and `candidate-downgrade`

The opportunity classes are directional. They are worth testing, but they are not proven waste.

## Default data sources

By default, Xerg looks for:

- gateway logs at `/tmp/openclaw/openclaw-*.log`
- session transcripts at `~/.openclaw/agents/*/sessions/*.jsonl`

If your data lives elsewhere, use `--log-file` and `--sessions-dir`.

## Start here

1. [Install Xerg](/installation).
2. [Run the quickstart](/quickstart).
3. If the data lives elsewhere, start with [doctor](/doctor) and [remote audits](/remote-audits).
4. Learn the core model in [waste taxonomy](/waste-taxonomy), [metrics](/metrics), and [how it works](/how-it-works).
5. When you want automation, use [CI gates](/ci-gates) or [push summaries](/auth-and-push).

## Important v0 limits

- Cost per outcome is intentionally unavailable in v0.
- Opportunity findings are recommendations to test, not proven waste.
- Xerg stores economic metadata and audit summaries locally. It does not store prompt or response content.
